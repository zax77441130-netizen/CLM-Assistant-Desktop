{
  "name": "@clm/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "test": "vitest run",
    "clean:electron": "node -e \"const fs=require('node:fs'); for (const p of ['dist/main','dist/preload','dist/shared','dist/renderer','dist/apps','dist/packages']) fs.rmSync(p,{recursive:true,force:true});\"",
    "build": "npm run clean:electron && tsc -p tsconfig.electron.json && vite build --config vite.preload.config.ts && vite build",
    "electron": "electron dist/main/main.js",
    "build:desktop": "electron-builder --win nsis --x64"
  },
  "dependencies": {
    "@clm/contracts": "0.1.0",
    "@vitejs/plugin-react": "6.1.1",
    "vite": "8.2.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "lucide-react": "1.35.0"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "electron": "44.0.0",
    "electron-builder": "26.15.3",
    "vitest": "4.1.11",
    "jsdom": "30.0.1",
    "typescript": "5.5.4"
  },
  "build": {
    "appId": "studio.clm.assistant",
    "productName": "CLM Assistant Desktop",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../services/agent_runtime/dist",
        "to": "agent_runtime",
        "filter": ["**/*"]
      }
    ]
  }
}
