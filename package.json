{
  "name": "clm-assistant-desktop",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/desktop",
    "packages/contracts"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspace @clm/contracts && npm run typecheck --workspace @clm/desktop",
    "test": "npm run test --workspace @clm/desktop",
    "build": "npm run build --workspace @clm/contracts && npm run build --workspace @clm/desktop",
    "dev": "npm run dev --workspace @clm/desktop"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
